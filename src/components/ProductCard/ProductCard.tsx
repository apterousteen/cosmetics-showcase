import type { Product } from '../../api/types';

/** Карточка товара: фото -> название -> комментарий -> цена -> чип (ТЗ §4.2). */
type ProductCardProps = {
  product: Product;
};

export function ProductCard(_props: ProductCardProps) {
  // TODO(этап 4): верстка карточки + фолбэк картинки.
  return null;
}
