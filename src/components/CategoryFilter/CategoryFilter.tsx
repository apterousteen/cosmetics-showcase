/** MultiSelect по категориям (ТЗ §4.1). */
type CategoryFilterProps = {
  categories: string[];
  value: string[];
  onChange: (value: string[]) => void;
};

export function CategoryFilter(_props: CategoryFilterProps) {
  // TODO(этап 5): MultiSelect, clearable, логика ИЛИ.
  return null;
}
