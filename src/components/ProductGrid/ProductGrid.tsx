import type { Product } from '../../api/types';

/** Адаптивная сетка карточек (ТЗ §4.7). */
type ProductGridProps = {
  products: Product[];
};

export function ProductGrid(_props: ProductGridProps) {
  // TODO(этап 6): SimpleGrid + ProductCard.
  return null;
}
