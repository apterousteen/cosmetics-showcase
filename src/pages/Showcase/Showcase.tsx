/**
 * Главный экран: загрузка данных -> фильтр + сетка.
 * Оркестрирует состояния (загрузка / ошибка / нет данных / пустой фильтр).
 */
export function Showcase() {
  // TODO(этапы 2-6): загрузка, фильтр, сетка, состояния.
  return null;
}
